name: flutter_pollfish_example
description: Demonstrates how to use the Pollfish flutter plugin.

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_pollfish:
    path: ../

flutter:

  uses-material-design: true
